<Page x:Class="BackupEssentials.Pages.BackupLocations"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:sys="clr-namespace:System;assembly=mscorlib"
      xmlns:backup="clr-namespace:BackupEssentials.Backup"
      mc:Ignorable="d" 
      d:DesignWidth="{StaticResource PageWidth}" d:DesignHeight="{StaticResource PageHeight}"
      Title="Backup Locations">
    
    <Page.Resources>
        <sys:Double x:Key="LocationListItemHeight">46</sys:Double>
    </Page.Resources>

    <Grid Background="#FF2E2E2E">
        <Grid.RowDefinitions>
            <RowDefinition Height="48"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <Grid.Resources>
            <Style TargetType="ListView">
                <Setter Property="Margin" Value="8"/>
                <Setter Property="BorderThickness" Value="2"/>
                <Setter Property="BorderBrush" Value="#FF252525"/>
                <Setter Property="Background" Value="#FF444444"/>
            </Style>
            <Style TargetType="ListViewItem">
                <Setter Property="Padding" Value="8,4"/>
                <Setter Property="AllowDrop" Value="True"/>
            </Style>
        </Grid.Resources>
        
        <StackPanel Orientation="Horizontal">
            <StackPanel.Resources>
                <Style TargetType="Button">
                    <Setter Property="Margin" Value="8,8,0,0"/>
                    <Setter Property="VerticalAlignment" Value="Center"/>
                    <Setter Property="Width" Value="120"/>
                    <Setter Property="Height" Value="40"/>
                    <Setter Property="Template" Value="{StaticResource ButtonStyleDefault}"/>
                </Style>
            </StackPanel.Resources>
            <Button Content="Add Location"/>
            <Button Content="Edit"/>
            <Button Content="Remove"/>
        </StackPanel>

        <ListView Name="LocationsListView" Grid.Row="1" PreviewMouseLeftButtonDown="ListStartDragging" MouseMove="ListMouseMove" PreviewMouseLeftButtonUp="ListStopDragging">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Width="740" Height="{StaticResource LocationListItemHeight}" SnapsToDevicePixels="True">
                        <TextBlock Foreground="White" Text="{Binding Name}" VerticalAlignment="Top" FontSize="20"/>
                        <TextBlock Foreground="White" Text="{Binding Directory}" VerticalAlignment="Bottom" FontSize="16"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.Items>
                <backup:BackupLocation Name="Test1" Directory="C:\Abc"/>
                <backup:BackupLocation Name="Test2" Directory="C:\Abc"/>
                <backup:BackupLocation Name="Test3" Directory="C:\Abc"/>
                <backup:BackupLocation Name="Test4" Directory="C:\Abc"/>
                <backup:BackupLocation Name="Test5" Directory="C:\Abc"/>
                <backup:BackupLocation Name="Test6" Directory="C:\Abc"/>
                <backup:BackupLocation Name="Test7" Directory="C:\Abc"/>
                <backup:BackupLocation Name="Test8" Directory="C:\Abc"/>
            </ListView.Items>
        </ListView>
    </Grid>
</Page>
